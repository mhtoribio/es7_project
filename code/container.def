Bootstrap: docker
From: ubuntu:24.04

%environment
    export VIRTUAL_ENV=/opt/venv
    export PATH="$VIRTUAL_ENV/bin:$PATH"
    export MPLBACKEND=Agg
    export PYTHONUNBUFFERED=1
    export PYTHONDONTWRITEBYTECODE=1

%post
    set -eux
    export DEBIAN_FRONTEND=noninteractive
    apt-get update
    apt-get install -y --no-install-recommends \
        python3 python3-venv python3-pip python3-dev \
        build-essential pkg-config \
        libsndfile1 ffmpeg ca-certificates
    rm -rf /var/lib/apt/lists/*

    python3 -m venv /opt/venv
    /opt/venv/bin/pip install --no-cache-dir --upgrade pip setuptools wheel

    # build tools first so compiles work
    /opt/venv/bin/pip install --no-cache-dir cython

    # core pkgs
    /opt/venv/bin/pip install --no-cache-dir numpy scipy matplotlib tqdm pyroomacoustics pystoi

    # pesq: use venv and disable build isolation so it sees cython
    /opt/venv/bin/pip install --no-cache-dir --no-build-isolation pesq

    # extras implied by imports
    /opt/venv/bin/pip install --no-cache-dir librosa onnxruntime pandas soundfile requests

%test
    # Test that Python works
    python3 --version
